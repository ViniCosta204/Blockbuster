package projetoesboco;

import java.util.ArrayList;
import java.util.Arrays;

import projetoesboco.mainPage;

public class Main {

	public static void main(String[] args) {
		new mainPage();
		//FilmDbConnection c = new FilmDbConnection();
		//c.insert(new Film<Genre>(1, "Terminator", 5,
		//		new Comedy(ClassificationEnum.Fourteen), true, "Cameron"));
		//ArrayList<Film<Genre>> find = c.findAll();
		//System.out.println(Arrays.toString(find.toArray()));
		//System.out.println(c.findOne(1));
		//c.update(new Film<Genre>(1, "Back to the Future", 10.50,new Action(ClassificationEnum.R), false, "Zemeckis"));
		//c.delete(1);
		//c.update(new Film<Genre>(1, "Back to the Future", 10.50,new Action(ClassificationEnum.R), false, "Zemeckis"));

	}
}